filedump.exe    is the Windows executable

filedump.linux  is the Linux (x86_64) executable

filedump        is the z/OS (OMVS) executable

On z/OS and Linux, remember to mark the uploaded binary file as executable, for example:
```
$ chmod +x filedump
```
